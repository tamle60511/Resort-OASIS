import type { NextPage } from "next";
import ReservationConfirmedPage from "@/templates/ReservationConfirmedPage";

const ReservationConfirmed: NextPage = () => {
    return <ReservationConfirmedPage />;
};

export default ReservationConfirmed;
